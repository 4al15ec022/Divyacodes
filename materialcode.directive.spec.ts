import { MaterialcodeDirective } from './materialcode.directive';

describe('MaterialcodeDirective', () => {
  it('should create an instance', () => {
    const directive = new MaterialcodeDirective();
    expect(directive).toBeTruthy();
  });
});
